{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "npx --yes pnpm run typecheck && npx --yes pnpm --filter \"./artifacts/api-server\" --filter \"./artifacts/sukoon\" run build && node scripts/init-db.mjs && node scripts/update-price.mjs && node scripts/postbuild.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "npx --yes pnpm run typecheck:libs && npx --yes pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "hostinger-build": "npx --yes pnpm install --force && npx --yes pnpm run build",
    "start": "node scripts/init-db.mjs && node artifacts/api-server/dist/index.mjs"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1",
    "@libsql/client": "^0.14.0",
    "libsql": "^0.5.29"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "prettier": "^3.9.5",
    "typescript": "~5.9.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3"
    ]
  }
}
